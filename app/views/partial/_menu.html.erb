<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%inicio_index_path%>">Mi Comunidad</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
          <% if usuario_signed_in?%>
            <% if can? :all, Usuario %>
            <li><a href="<%= new_usuario_registration_path %>">Usuario</a></li>
            <% end %>
            <% if can? :read, Cliente %>
            <li><a href="<%= clientes_path %>">Cliente</a></li>
            <% end %>
            <li><a href="<%= ubicacions_path %>">Ubicacion</a></li>
            <li><a href="<%= unidads_path %>">Unidad</a></li>
          <% end%>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        
        <% if usuario_signed_in?%>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_usuario.email%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            
            <li><a href="<%=edit_usuario_registration_path %>"><span class="glyphicon glyphicon-edit"></span> Editar Usuario</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="<%=destroy_usuario_session_path %>" data-method="delete"><span class="glyphicon glyphicon-log-out"> Cerrar Sesión</a></li>
          </ul>
        </li>
        <% else%>
        <li><a href="<%=new_usuario_session_path %>"><span class="glyphicon glyphicon-log-in"></span> Iniciar Sesión</a></li>
      
        <% end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>